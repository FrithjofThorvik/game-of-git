import type { NextPage } from 'next';

/**
 * Game component
 * @return {JSX.Element} - The JSX code for Game component
 */
const Game: NextPage = (): JSX.Element => {
  return <div>Game</div>;
};

export default Game;
