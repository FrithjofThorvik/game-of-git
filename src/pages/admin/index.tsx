import type { NextPage } from 'next';

/**
 * Admin component
 * @return {JSX.Element} - The JSX code for Admin component
 */
const Admin: NextPage = (): JSX.Element => {
  return <div>Admin</div>;
};

export default Admin;
